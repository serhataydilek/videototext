OPENAI_API_KEY=
# Timestamped segments use whisper-1 because it supports verbose_json segment output.
OPENAI_TRANSCRIBE_MODEL=whisper-1
OPENAI_SUMMARY_MODEL=gpt-5-mini
# Optional overrides if the binaries are not on PATH.
YTDLP_PATH=yt-dlp
FFMPEG_PATH=ffmpeg
